# This folder is not used for the main site

The main site pages are in `/src/app/`.

This folder was causing conflicts with Next.js auto-detection.
The app subdomain files have been moved to `/_app-subdomain/`.

To deploy the app subdomain, use root directory: `_app-subdomain`
